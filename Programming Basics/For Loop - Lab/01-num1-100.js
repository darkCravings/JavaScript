function numbers() {

    for (let index = 1; index <= 100; index++) {        // Повтаря цикъла докато индекс е <= 100 и след всяко повтаряне добавя +1;
        console.log(index);
    }
}

numbers();